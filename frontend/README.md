This is a minimal scaffolded Vite + React + TypeScript frontend used for local development.

Commands:

- `npm install` — install dependencies
- `npm run dev` — start dev server on http://localhost:5173
