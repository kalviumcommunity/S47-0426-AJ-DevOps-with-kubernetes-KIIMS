# Copy this file to .env and fill with secure values for local development
# Do NOT commit secrets for production

# JSON Web Token signing secret (used for access tokens)
JWT_SECRET=your_jwt_secret_here

# Session signing secret (used for refresh tokens / session storage)
SESSION_SECRET=your_session_secret_here

# Optional: enable the simple in-memory registrar router
#SIMPLE_REGISTRAR=true
